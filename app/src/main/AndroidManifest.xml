<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.mobilemakers.compareprice" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MarketListActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MarketCreationActivity"
            android:label="@string/title_activity_super_market_selection" >
        </activity>
        <activity
            android:name=".ProductListActivity"
            android:label="@string/title_activity_product_list" >
        </activity>
        <activity
            android:name=".ProductCreationActivity"
            android:label="@string/title_activity_product_creation" >
        </activity>
        <activity android:name=".AppSettingsActivity"
            android:label="@string/title_activity_app_settings"
            android:parentActivityName=".MarketListActivity"
            android:theme="@android:style/Theme.Holo.Light.DarkActionBar"
            android:icon="@drawable/android_market_fashion"/>
    </application>

</manifest>
